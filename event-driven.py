# Print out all the state names from the csv# Showing an "event-driven" styleimport systry:  f = open('FAKE_2012_US_election_state.csv', 'r')except IOError:  print "Error: couldn't open the file"  sys.exit()print "Opened file:"all_lines = f.readlines()for line in all_lines:  columns = line.split(",")  print "  "+columns[1]print "done ("+str(len(all_lines))+" lines)"